/** Conservé pour compatibilité : redirige vers le vrai générateur de QR.
 *  (Anciennement un faux QR SVG décoratif — désormais un QR réel scannable.) */
export { QRCode as FakeQR } from '../../components/device/QRCode';
